import 'package:flutter/cupertino.dart';

class MobileFooter extends StatelessWidget {
  const MobileFooter({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 400,
      child: Text('MobileFooter'),
    );
  }
}
